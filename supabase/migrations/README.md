# Migrations deprecated

Incremental migrations have been squashed into a single schema file.

**Run [`../schema.sql`](../schema.sql) once** in the Supabase SQL Editor on a fresh project. No migration files are needed.
